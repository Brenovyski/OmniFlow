import { PlaceholderPage } from "@/components/placeholder-page";

export function TransactionsPage() {
  return (
    <PlaceholderPage
      title="Transactions"
      subtitle="Every flow in, out, and around — searchable, filterable, editable."
    />
  );
}
